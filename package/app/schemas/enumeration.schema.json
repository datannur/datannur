{
  "$schema": "./__meta__.schema.json",
  "$id": "https://dev.datannur.com/schemas/enumeration.schema.json",
  "x-db": "data",
  "title": "enumeration",
  "description": "Enumerations",
  "x-description-fr": "Énumérations",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["id"],
    "additionalProperties": false,
    "properties": {
      "id": {
        "type": "string",
        "description": "Primary key of the enumeration",
        "x-description-fr": "Clé primaire de l'énumération"
      },
      "folder_id": {
        "type": ["string", "number", "null"],
        "description": "Folder key",
        "x-description-fr": "Clé du dossier"
      },
      "name": {
        "type": ["string", "null"],
        "description": "Enumeration name",
        "x-description-fr": "Nom de l'énumération"
      },
      "description": {
        "type": ["string", "null"],
        "description": "Enumeration description",
        "x-description-fr": "Description de l'énumération"
      },
      "type": {
        "type": ["string", "null"],
        "description": "Enumeration datatype",
        "x-description-fr": "Datatype de l'énumération"
      }
    }
  }
}
