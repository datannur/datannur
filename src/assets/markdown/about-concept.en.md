Business glossary concepts define and standardize the terminology used in the data.

They can be associated with variables, tags, and docs.

mermaid(
$concept $recursive
$concept --> $variable
$concept <--> $tag
$concept <--> $doc
);
