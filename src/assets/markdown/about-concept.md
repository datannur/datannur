Les concepts du glossaire métier définissent et normalisent la terminologie utilisée dans les données.

Ils peuvent être associés à des variables, des mots clés et des docs.

mermaid(
$concept $recursive
$concept --> $variable
$concept <--> $tag
$concept <--> $doc
);
