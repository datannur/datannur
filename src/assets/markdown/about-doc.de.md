Eine Doc (Dokumentationselement) kann eine PDF- oder Markdown-Datei sein (das für Readme-Dateien übliche Format).

Sie kann mit zahlreichen Organisationen, Ordnern, Datensätzen und Tags verknüpft werden.

mermaid(
$organization <--> $doc
$folder <--> $doc
$tag <--> $doc
$dataset <--> $doc
);
