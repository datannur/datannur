Tags can be assigned to organizations, folders, datasets, and variables.

Docs can also be assigned to them.

mermaid(
$tag $recursive
$organization <--> $tag
$folder <--> $tag
$dataset <--> $tag
$variable <--> $tag
$tag <--> $doc
);
