Una doc (elemento di documentazione) può essere un file PDF o Markdown (il formato comunemente usato per i file readme).

Può essere associata a numerose organizzazioni, cartelle, dataset e parole chiave.

mermaid(
$organization <--> $doc
$folder <--> $doc
$tag <--> $doc
$dataset <--> $doc
);
